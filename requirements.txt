# Web deployment dependencies (Render / Railway).
# The heavy face-recognition stack (tensorflow, deepface, opencv, etc.) is
# NOT included here because it cannot run on a cloud host without a webcam and
# would break the build size/memory limits. Those live in requirements-ml.txt
# for local development of the capture/recognition features.
Flask>=3.0
flask-cors>=5.0
gunicorn>=23.0
mysql-connector-python>=9.0
openpyxl>=3.1
python-dotenv>=1.0
